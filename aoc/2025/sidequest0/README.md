# Side Quest 0

# How SOC-mas became EAST-mas: Hopper's Origins


https://static-labs.tryhackme.cloud/apps/hoppers-invitation/

Note that you need to solve the CORS issue to access the invite. 

If your blob starts with `r` it is incorrect. 
Supposedly the correct one starts with `h`. 

On linux machines, the flags are at `/user.txt` and `/root/root.txt`
On windows machines, the flags are in `C:\user.txt` and `C:\Users\Administrator\root.txt`